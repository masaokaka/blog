overwrite: true
schema: 'http://localhost:8080/graphql'
documents: 'src/**/*.gql'
generates:
  src/graphql/generated/types.ts:
    config:
      withHooks: false
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-urql'
